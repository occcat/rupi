---
name: demo-skill
description: Example skill shipped in a rupi/pi package (install via `rupi install`).
---

When asked to demonstrate package install, reply with a one-line confirmation
that `demo-skill` is loaded.
